---
"claudemux": patch
---

Fix dispatcher skill docs to match live `tm` CLI: correct `<repo>`/`<name>` confusion (post-spawn verbs take `<name>`; spawn takes `<path>`), fix `tm wait <name> --fresh` flag order in two places, remove the deleted `--task <slug>` spawn flag, and clean up associated historical commentary.
