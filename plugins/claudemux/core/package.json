{
  "name": "claudemux-core",
  "version": "1.0.0",
  "private": true,
  "description": "The claudemux orchestration core (the `next` 1.0.0 line) — the verb dispatch and native verb implementations behind the `tm` CLI.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.7.0"
  },
  "imports": {
    "#ws": "./third_party/ws/wrapper.mjs"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint:dead-code": "node scripts/check-dead-code.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "knip": "5.80.2",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
