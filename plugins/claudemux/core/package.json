{
  "name": "claudemux-core",
  "version": "0.0.0",
  "private": true,
  "description": "The claudemux MCP-native orchestration core (the `next` line, 1.0.0-beta.0). Phase A: a resident process that fronts `tm` over MCP.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "bun install --no-summary && bun run src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
