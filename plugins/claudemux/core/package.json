{
  "name": "claudemux-core",
  "version": "0.0.0",
  "private": true,
  "description": "The claudemux orchestration core (the `next` line, 1.0.0-beta.0) — the verb dispatch and native verb implementations behind the `tm` CLI.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
