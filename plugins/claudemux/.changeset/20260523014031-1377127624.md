minor

migrate the hot-path verbs (spawn/send/wait/compact/resume/doctor) to native TypeScript; add a Node CLI wrapper at core/bin/tm so the live-teammate suite can re-aim at native via CLAUDEMUX_TM; fix encodeProjectDir to match Claude Code's real folding rule
