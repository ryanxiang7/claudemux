major

zero-install: tm runs the TS core directly via `node --experimental-transform-types`; vendor ws under `core/third_party/`; drop `dist/` and `npm install` (BREAKING — `npm run build` and `core/dist/cli.mjs` are gone, contributor workflow changes); minimum Node bumps from 22.0 to 22.7
