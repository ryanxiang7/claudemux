# claudemux changelog

## 1.0.0-beta.2 — 2026-05-25

- (patch) tm history list mode shows the full canonical sid/thread-id (was an 8-char prefix) — at both the cross-engine renderer and each engine's raw fallback path — so it pastes cleanly into tm resume; tm resume attaches an actionable "looks like a prefix" hint for both engines when the caller pastes a partial canonical-dashed id, replacing the misleading "wrong repo" error

## 1.0.0-beta.1 — 2026-05-25

- (minor) Add the resident MCP-native orchestration core (next line, Phase A): a Bun process that fronts the tm verb set over MCP, with a persistent teammate registry and a resident idle subscription
- (minor) core: migrate the ls and last verbs to native code, behind a tm-conformance harness
- (minor) core: migrate the ctx verb to native code, with jsonl transcript parsing
- (minor) core: migrate the states verb to native code
- (minor) core: migrate the mem verb to native code
- (minor) core: migrate the history verb to native code
- (minor) core: migrate the status and poll verbs to native code
- (minor) core: migrate the kill verb to native code
- (minor) core: migrate the archive verb to native code
- (minor) core: migrate the reload verb to native code
- (patch) core: correct conformance divergences found in review
- (patch) core: introduce the Node CLI front end
- (patch) core: remove the resident MCP server scaffolding
- (patch) core: migrate the runtime from Bun to Node
- (patch) core: harden the CLI entry-point guard for symlinked launch
- (minor) migrate the hot-path verbs (spawn/send/wait/compact/resume/doctor) to native TypeScript; add a Node CLI wrapper at core/bin/tm so the live-teammate suite can re-aim at native via CLAUDEMUX_TM; fix encodeProjectDir to match Claude Code's real folding rule
- (patch) address code-review findings on the hot-path verbs (compact stderr passthrough, sendKeys exit-code propagation, $PWD fallback, empty-value parity)
- (minor) retire the bash bin/tm: Node CLI bundle is the production entry, conformance harness moves to golden files
- (patch) address /code-review findings on the bash retirement
- (patch) pin conformance harness timezone to UTC so goldens stay byte-stable across CI
- (patch) absorb the macOS-vs-Linux /tmp symlink difference in conformance goldens
- (patch) also substitute the literal-encoded scratch path so archive/ctx goldens match Linux
- (minor) vendor codex app-server protocol schema (generate-ts output) as ground truth for the stage 4 driver
- (minor) add codex-daemon registry path builders, drop stage 3 dead state-dir helpers
- (minor) pin codex app-server response envelope (no jsonrpc field) with a captured fixture and schema tests
- (minor) add CodexWsClient — the WebSocket JSON-RPC client for codex app-server
- (minor) add codex daemon supervisor — spawn-detached, liveness probe, reap
- (minor) tm verbs: route codex-<n> teammates through the codex driver on spawn/send/wait/kill
- (minor) tm doctor: report codex teammates, reap dead-pid orphans
- (minor) ask mode: tm ask borrows an idle codex teammate, runs one turn on a fresh thread, returns the teammate
- (minor) add live-codex integration suite (smoke + opt-in turn-spending), fix ESM bundle CJS require for ws
- (minor) codex driver: tm send --no-wait makes tm wait reachable; tm ask uses ephemeral thread to avoid daemon-side thread leak
- (minor) /code-review fixes: codexAsk lock leak, env vars use || not ?? to match cli.ts convention, error format uniformity, ws server-request promise containment
- (patch) hotfix: codex turn round-trip works end-to-end (perMessageDeflate, thread/resume on reconnect, daemon stderr logging)
- (patch) kill codex daemon by process group, not single pid — closes the orphan path the dispatcher hit
- (patch) /code-review followups on the orphan fix — poll-instead-of-sleep, fake-spawn diagnostics, docstring nit
- (patch) codex: turn replies now carry the item stream — fixes empty items / itemsView=notLoaded
- (patch) codex: collector docstring + idempotent awaitTurn + honest itemsView (review follow-up)
- (minor) decision multi-engine-tui-architecture: Engine interface, TeammateRecord JSON, supersedes codex-engine-flag §2 §3
- (patch) decision multi-engine-tui-architecture amend: verb default impls + fleet-visibility decoupling + process decisions + path normalization
- (minor) Phase 1: Engine interface + verb skeletons + TeammateRecord abstraction
- (minor) Phase 2b: codex engine migration to engines/codex/*
- (minor) Phase 2a-1: shared persistence + identity infra, ClaudeEngine class, and EngineRegistry-routed fleet visibility (ls/states/status) + verbs/archive.ts. tm kill stays on NATIVE_VERBS until Phase 2b lands CodexEngine.
- (patch) fix codex engine review blockers and Phase 2a rebase wiring
- (patch) Phase 2a-1 review fixes: identity-store.list includes the codex registry root via path-reconstruction (P0: codex/foo nested base record now enumerated); validateTeammateName allows __ in flat names so legacy teammate-flow__1 stays reachable (P1); claude-engine.status propagates list-sessions and capture-pane failures instead of leaking pane ids to stdout (P2).
- (patch) Phase 2b follow-up: codex name validation + identity-store reservation + unbounded timeout + busy/borrow + doctor base cleanup
- (patch) verbs: switch conformance to runCli + restore states rich columns + idempotent kill
- (patch) core: migrate last/mem/ctx bodies into engines/claude/ — ClaudeEngine no longer facades these
- (patch) core: extract hot-path helpers into engines/claude submodules
- (patch) core: migrate 8 hot-path/diagnostic verb bodies into engines/claude submodules
- (patch) core: ClaudeEngine calls claude verb modules directly, no more callNative seam
- (patch) core: delete native.ts; cli.ts dispatch flips all verbs through their own modules
- (major) core: remove --no-wait flag end-to-end (breaking)
- (patch) core: consolidate plugin-root path math into core/src/plugin-root.ts
- (patch) Render Codex teammates in rich states rows
- (major) Multi-engine TUI architecture: claude and codex teammates dispatched through a unified Engine API, with TeammateRecord identity routing
- (minor) Add Codex liveness status, last reply, and context usage, with rollout lookup and probe-timeout hardening
- (minor) codex resume verb relaunches thread-id backed teammates
- (patch) polish codex resume CLI output
- (minor) tm history lists codex thread ids
- (patch) tm history codex follow-up
- (patch) tm states preview for codex
- (minor) tm resume native auto-pick (claude --continue + codex thread/list RPC)
- (patch) fix tm ctx/reload --all codex fan-out + history cwd fallback
- (patch) fix .last not initialized on tm resume
- (patch) show mixed-engine tm history rows and skip task notifications
- (patch) resume probes both engines when called without id and a base record
- (major) zero-install: tm runs the TS core directly via `node --experimental-transform-types`; vendor ws under `core/third_party/`; drop `dist/` and `npm install` (BREAKING — `npm run build` and `core/dist/cli.mjs` are gone, contributor workflow changes); minimum Node bumps from 22.0 to 22.7
- (patch) rename ADR files to topic slugs
- (patch) comments+help: rewrite decision NNNN refs to slugs; check.sh adds decisions-index and NNNN-guard checks
- (major) Breaking exit-code split: `tm send` / `tm wait` / `tm spawn --prompt` / `tm compact` now exit `124` (GNU `timeout(1)` convention) when their `--timeout` elapses without a Stop / PostCompact hook (was `1`), while true failures (no session, no sid, sendKeys dead) keep `1`. The wait-expiry path also re-probes the teammate before returning `124` so a session that died mid-wait surfaces as `1` instead of being mis-reported as "still running". Codex sync-wait expiries align on `124` and `tm wait` now uses a `thread/read` backfill alongside the live subscription, surfacing any TERMINAL turn (completed / failed / interrupted) that resolved in the gap between a previous `tm send` timing out and the new wait subscribing — so late failures map to exit `1` instead of spinning to `124`, identical to the live-notification path.
- (minor) Add static dead-code guard for claudemux core
- (minor) Migrate teammate routing to identity records
- (patch) Layer the core CLI dispatcher
- (patch) Unify core path builders
- (patch) Split Codex compatibility verbs and remove dead exports
- (minor) Improve tm spawn/resume/history UX (U2/U3/U4/U6/U7): clearer --task error, naming-style suggestion when the legacy codex- prefix is used on creation paths, verb --engine audit, claude history dedup, UUID-version short-circuit in history detail.
- (minor) Document Codex dispatcher workflows
- (patch) Align Codex `tm states` LAST age with the latest assistant reply timestamp.
- (major) Align Codex sync output with Claude-style summaries
