# notes/

Long-term local notes you curate by hand (alone or together with the dispatcher).

- Free-form filenames; pick whatever helps you find it later.
- **Not** auto-imported into the dispatcher's context — the dispatcher reads a file deliberately when it's relevant.
- Use this when something is worth keeping by design. For dispatcher-generated intermediate output (triage tables, research dumps), see `../artifacts/` instead.
