{
  "name": "claude-channel-feishu",
  "version": "0.1.0",
  "description": "Feishu (飞书) channel for Claude Code.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "npm install --silent --no-audit --no-fund >&2 && tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.64.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
