# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*~

# Backup files
*.bak
*.bak-*

# Dependencies — each plugin keeps its own node_modules under its own dir;
# the repo root should never have one, but a stray `npx`-from-root run leaves
# one behind. Ignore so the next stray invocation can't accidentally commit it.
node_modules/

# Local Claude Code scheduling state (per-machine)
.claude/
